from __future__ import annotations

from pathlib import Path


def parse_with_local_fallback(pdf_path: Path) -> str:
    raise NotImplementedError("Docling/PyMuPDF4LLM fallback parsing is planned for M2.")

